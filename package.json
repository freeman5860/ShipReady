{
  "name": "shipready",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS landing page audit MVP for indie developers.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/audit.js"
  },
  "engines": {
    "node": ">=18"
  }
}
