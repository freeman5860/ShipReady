{
  "name": "shipready",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS landing page audit MVP for indie developers.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/audit.js && node --check src/store.js && node --check api/server.js",
    "vercel-build": "npm run check"
  },
  "engines": {
    "node": ">=18"
  }
}
